# API dependencies stubs - placeholder session dependency for routes validation
def get_session():
    """
    Returns standard session metadata for REST routes.
    """
    return {"user_id": "active_user", "workspace_id": "global_workspace"}
