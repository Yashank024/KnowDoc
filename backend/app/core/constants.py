# Global System Constants for KnowDoc AI Workspace

# Upload limits and formats
ALLOWED_EXTENSIONS = {".jpg", ".jpeg", ".png", ".bmp", ".tiff", ".pdf"}

# Chunker sliding window width configurations
DEFAULT_CHUNK_SIZE = 150
DEFAULT_CHUNK_OVERLAP = 40

# Local vector search metric limit defaults
DEFAULT_RAG_K = 4
COGS_SIMILARITY_THRESHOLD = 0.35
